import { CartDropdownContext } from "../../contexts/cart-dropdown.context";

const Checkout = () => {
  // Grab the cart items from context
  // Loop through the cart contents and render using checkout items component
};

export default Checkout;
